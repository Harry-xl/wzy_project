# 星伴（StarPal）— 智能学习平台 项目文档

> 版本：0.2.5 | 日期：2026-07-29

---

## 一、项目概述

星伴是一个**计算机网络主题的智能刷题 + AI 辅导平台**，采用 Python Flask 后端 + 原生 JavaScript 前端的轻量架构，集成 DeepSeek 大模型提供 AI 题目讲解和智能对话能力。

### 核心功能

- **智能刷题**：支持游客随机模式与登录用户的个性化推荐模式
- **能力画像**：基于答题数据自动生成用户各知识点熟练度画像（雷达图/条形图/环形图）
- **AI 讲解**：每道题目支持流式 AI 讲解，包含解题思路、知识点、易错点分析
- **AI 伴学**：内置计算机网络助教角色，支持多轮对话、Markdown 渲染、代码高亮
- **错题管理**：错题本支持分页浏览、排序、错题重做、同知识点/同难度重练
- **管理后台**：题目 CRUD、批量导入（JSON/CSV）、数据统计面板
- **自动清理**：后台定时清理过期答题记录和学习会话数据

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Python 3.11 + Flask 2.0.1 | REST API，端口 3001 |
| 数据库 | MySQL 8.x | 数据库名 `wzyProjectDb`，连接池 |
| 数据库驱动 | mysql-connector-python 8.0.26 | 含连接池功能 |
| 密码安全 | Werkzeug PBKDF2-SHA256 | 支持明文密码自动迁移为哈希 |
| AI 模型 | DeepSeek Chat API | 同步 + SSE 流式两种调用方式 |
| 前端 | 原生 HTML/CSS/JS | 无框架，ES6 类，iframe 嵌入架构 |
| 前端图标 | RemixIcon | 开源图标库 |
| Markdown | marked.js + Prism.js + KaTeX | 代码高亮 + 数学公式渲染 |
| 构建工具 | 无 | 纯静态文件，无需编译打包 |

---

## 三、项目目录结构

```
项目根目录/
├── server/
│   ├── app.py                  # ★ Flask 主后端（核心，1370 行）
│   ├── auth.js                 # 旧 Express 认证路由（已废弃）
│   └── index.js                # 旧 Express 入口（已废弃）
│
├── database/
│   ├── db_connector.py         # MySQL 连接池封装
│   ├── create_db.sql           # 建库建表 DDL
│   ├── insert_test_data.sql    # 测试数据
│   ├── init_db.py              # 数据库初始化脚本
│   ├── add_user_strength.sql   # user_strength 列迁移 DDL
│   └── clear_user_answers.py   # 答题记录清理脚本
│
├── AI_operate/                 # AI 业务逻辑模块
│   ├── exam.py                 # 选题策略（随机/个性化/筛选）
│   ├── Ability_Profile.py      # 用户能力画像系统
│   ├── deepseek_chat.py        # DeepSeek API 封装（同步 + 流式）
│   ├── start_exam.py           # 命令行做题入口
│   ├── test_profile.py         # 用户画像查看工具
│   ├── view_profiles.py        # 全量用户画像展示
│   ├── check_strength.py       # 用户实力查看工具
│   └── update_user_strength.py # user_strength 字段迁移脚本
│
├── deepseek_importer/          # DOCX 题目批量导入工具
│   ├── main.py                 # 导入编排器（支持断点续传）
│   ├── llm_parser.py           # LLM 题目提取（Stage 1）
│   ├── docx_reader.py          # DOCX 文档解析器
│   ├── llm_client.py           # DeepSeek API 客户端（含重试）
│   ├── mapper.py               # 题目格式化/映射
│   ├── config.py               # 配置（知识点列表、OSI 层级等）
│   ├── db.py                   # 数据库写入层
│   ├── docx_parser.py          # 旧版 DOCX 解析器（备用）
│   ├── problem.docx            # 待导入的题目文档样本
│   └── requirements.txt        # 导入工具独立依赖
│
├── static/                     # 前端静态资源
│   ├── login.html              # 登录/注册页（翻转卡片 UI）
│   ├── dashboard.html          # ★ 主仪表盘（iframe 容器）
│   ├── exam.html               # 独立做题页
│   ├── chat.html               # 独立 AI 聊天页
│   ├── admin.html              # 题目管理后台
│   ├── test_count.html         # 测试页面
│   ├── assets/
│   │   ├── starpal-style.css   # 共享样式（登录、仪表盘、做题）
│   │   ├── starpal-chat-style.css  # 聊天页样式
│   │   ├── admin-style.css     # 管理后台样式
│   │   ├── girl.png            # AI 头像
│   │   └── user-avatar.svg     # 默认用户头像
│   └── js/
│       ├── api.js              # API 客户端（HTTP 请求封装）
│       ├── auth.js             # 登录/注册逻辑
│       ├── storage.js          # localStorage 数据管理
│       ├── exam.js             # 做题页逻辑
│       ├── dashboard.js        # ★ 仪表盘主控（1500+ 行）
│       ├── chat.js             # 聊天页逻辑
│       ├── admin.js            # 管理后台逻辑
│       ├── renderer.js         # Markdown 渲染 + 代码高亮
│       └── utils.js            # 通用工具函数
│
├── requirements.txt            # Python 依赖
├── run_all.bat                 # 一键启动脚本
├── sample_problems.json        # 示例题目数据
├── check_db_performance.py     # 数据库性能诊断
└── .venv/                      # Python 虚拟环境
```

---

## 四、数据库设计

数据库名：`wzyProjectDb`

### 4.1 用户表（user）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | INT AUTO_INCREMENT | 主键 |
| email | VARCHAR(255) UNIQUE | 邮箱（登录账号） |
| name | VARCHAR(255) | 用户昵称 |
| password | VARCHAR(255) | PBKDF2-SHA256 哈希密码 |
| user_strength | FLOAT DEFAULT 0.5 | 用户整体实力（0.0~1.0） |
| created_at | TIMESTAMP | 注册时间 |

### 4.2 题库表（problems）

| 字段 | 类型 | 说明 |
|------|------|------|
| problem_id | INT AUTO_INCREMENT | 主键 |
| problem_num | VARCHAR(50) UNIQUE | 题目编号 |
| problem | TEXT | 题干内容 |
| answer | VARCHAR(500) | 正确答案 |
| difficulty | ENUM('简单','中等','困难') | 难度等级 |
| knowledge_point | VARCHAR(200) | 所属知识点 |
| osi_layer | VARCHAR(50) | OSI 层级（可选） |
| created_at | TIMESTAMP | 添加时间 |

### 4.3 能力画像表（ability_profile）

| 字段 | 类型 | 说明 |
|------|------|------|
| profile_id | INT AUTO_INCREMENT | 主键 |
| user_id | INT | 用户 ID（外键） |
| knowledge_point | VARCHAR(200) | 知识点 |
| proficiency_level | FLOAT DEFAULT 0.5 | 熟练度（0.0~1.0） |
| last_updated | TIMESTAMP | 最后更新时间 |

> 唯一约束：(user_id, knowledge_point)

### 4.4 答题记录表（user_answers）

| 字段 | 类型 | 说明 |
|------|------|------|
| answer_id | INT AUTO_INCREMENT | 主键 |
| user_id | INT | 用户 ID（外键） |
| problem_id | INT | 题目 ID（外键） |
| user_answer | VARCHAR(500) | 用户提交的答案 |
| is_correct | BOOLEAN | 是否正确 |
| answer_time | TIMESTAMP | 提交时间 |

### 4.5 学习会话表（learning_sessions）

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | INT AUTO_INCREMENT | 主键 |
| user_id | INT | 用户 ID（外键） |
| start_time | TIMESTAMP | 会话开始时间 |
| end_time | TIMESTAMP | 会话结束时间 |
| total_problems | INT | 总答题数 |
| correct_problems | INT | 正确答题数 |

> 24 小时内的连续答题会自动聚合到同一会话中

---

## 五、后端 API 接口文档

后端运行在 `http://127.0.0.1:3001`

### 5.1 认证接口

#### POST `/api/signup` — 用户注册

**请求体：**
```json
{
  "name": "用户昵称",
  "email": "user@example.com",
  "password": "登录密码"
}
```

**返回：**
```json
{ "success": true }
// 或
{ "success": false, "message": "邮箱已注册" }
```

> 密码使用 PBKDF2-SHA256 哈希后存储，日志中不打印明文密码。

#### POST `/api/login` — 用户登录

**请求体：**
```json
{
  "email": "user@example.com",
  "password": "登录密码"
}
```

**返回：**
```json
{
  "success": true,
  "user_id": 1,
  "name": "用户昵称"
}
```

> 兼容历史明文密码：首次登录时自动迁移为哈希。

### 5.2 题目接口

#### GET `/api/problems` — 获取题目

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | int | 否 | 用户 ID（不传或 <=0 为游客随机模式） |
| count | int | 否 | 题目数量（默认 5，最大 50） |
| stale_days | int | 否 | 久未做阈值天数（默认 30） |

**返回：**
```json
{
  "success": true,
  "problems": [
    {
      "problem_id": 1,
      "problem_num": "NET-001",
      "problem": "TCP 三次握手的过程是什么？",
      "answer": "SYN → SYN-ACK → ACK",
      "difficulty": "中等",
      "knowledge_point": "TCP 协议"
    }
  ]
}
```

#### GET `/api/problems/filter` — 按条件筛选题目

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | int | 是 | 用户 ID |
| count | int | 否 | 题目数量（默认 5） |
| knowledge_points | string | 否 | 知识点（逗号分隔） |
| difficulties | string | 否 | 难度（逗号分隔） |
| stale_days | int | 否 | 久未做阈值（默认 30） |

#### GET `/api/knowledge_points` — 获取全量知识点列表

**返回：**
```json
{
  "success": true,
  "items": ["TCP 协议", "IP 子网划分", "DNS 解析", ...]
}
```

### 5.3 答题接口

#### POST `/api/submit_answer` — 提交答案

**请求体：**
```json
{
  "user_id": 1,
  "problem_id": 10,
  "user_answer": "SYN → SYN-ACK → ACK"
}
```

**返回：**
```json
{
  "success": true,
  "is_correct": true,
  "correct_answer": "SYN → SYN-ACK → ACK"
}
```

> 登录用户提交后会自动执行 4 步流水线：记录答题 → 更新能力画像 → 聚合学习会话 → 刷新用户整体实力。游客（user_id ≤ 0）仅返回判题结果，不写入数据库。

### 5.4 用户画像接口

#### GET `/api/user_profile/<user_id>` — 获取用户能力画像

**返回：**
```json
{
  "success": true,
  "profile": {
    "name": "用户昵称",
    "email": "user@example.com",
    "user_strength": 0.72,
    "abilities": {
      "TCP 协议": 0.85,
      "IP 子网划分": 0.60,
      "DNS 解析": 0.45
    }
  },
  "analysis": "该学生在 TCP 协议方面表现优秀..."
}
```

> `analysis` 字段由 DeepSeek 大模型根据用户最近 10 条答题记录和整体画像生成。

#### GET `/api/user_strength_trend/<user_id>` — 获取实力趋势

**返回：**
```json
{
  "success": true,
  "trend": [
    { "t": "2026-07-20 14:30:00", "value": 0.55 },
    { "t": "2026-07-22 09:15:00", "value": 0.62 },
    { "t": "2026-07-25 16:45:00", "value": 0.72 }
  ]
}
```

> 数据来源于 `learning_sessions` 表，每个数据点代表一次会话的正确率，用于前端绘制折线图。

### 5.5 错题接口

#### GET `/api/wrong_answers/<user_id>` — 获取错题列表

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | int | 否 | 每页数量（默认 50，最大 200） |
| offset | int | 否 | 偏移量（默认 0） |
| sort_by | string | 否 | 排序方式：`time`（按时间）或 `knowledge_point`（按知识点） |

**返回：**
```json
{
  "success": true,
  "items": [
    {
      "answer_id": 1,
      "problem_id": 5,
      "problem_num": "NET-005",
      "problem": "CIDR 地址块 192.168.0.0/26 有多少个可用 IP？",
      "answer": "62",
      "difficulty": "中等",
      "knowledge_point": "IP 子网划分",
      "user_answer": "64",
      "is_correct": false,
      "answer_time": "2026-07-28 10:30:00",
      "redo_wrong_count": 2
    }
  ]
}
```

#### GET `/api/wrong_redo/<user_id>` — 错题重做选题

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| count | int | 否 | 题目数量（默认 10） |
| mode | string | 否 | 模式：`time` 或 `knowledge_point` |
| knowledge_point | string | 否 | 限定知识点（mode=knowledge_point 时生效） |

### 5.6 AI 接口

#### POST `/api/explain` — 同步 AI 讲解

**请求体：**
```json
{
  "problem_text": "TCP 三次握手的过程是什么？",
  "knowledge_point": "TCP 协议",
  "difficulty": "中等",
  "user_answer": "两次握手",
  "correct_answer": "SYN → SYN-ACK → ACK"
}
```

**返回：**
```json
{
  "success": true,
  "explanation": "## 考查知识点与思路\n\nTCP 三次握手是建立可靠连接的基础..."
}
```

#### POST `/api/explain/stream` — 流式 AI 讲解

参数与 `/api/explain` 相同，返回 `text/event-stream` 格式：

```
data: 正在生成讲解…

data: ## 考查知识点与思路

data: TCP 三次握手是建立可靠连接的基础...

data: [DONE]
```

> 前端通过 `fetch` + `ReadableStream` 读取，实现打字机效果，并支持随时中止。

#### POST `/api/chat` — AI 对话（流式）

**请求体：**
```json
{
  "message": "请解释 OSPF 和 BGP 的区别",
  "username": "user@example.com",
  "chat_id": "chat_xxx",
  "system_prompt": "自定义系统提示词（可选）"
}
```

**返回（SSE）：**
```
data: {"reply": ""}

data: {"reply": "## OSPF 与 BGP 对比\n\n### 1. 协议类型\nOSPF（Open Shortest Path First）是**内部网关协议（IGP）**..."}

data: {"reply": "", "done": true}
```

> 默认系统提示词设定 AI 为**计算机网络专业助教**，教学范围覆盖 OSI/TCP-IP 分层模型、以太网、IP/子网划分/CIDR、ARP/ICMP、路由交换协议（RIP/OSPF/BGP）、NAT、TCP/UDP、HTTP/HTTPS/DNS、TLS/证书、网络安全与排错等。

### 5.7 管理后台接口

| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/admin/problems` | GET | 题目列表（分页 + 搜索 + 筛选） |
| `/api/admin/problems` | POST | 创建新题目 |
| `/api/admin/problems/<id>` | PUT | 更新题目 |
| `/api/admin/problems/<id>` | DELETE | 删除题目（级联删除相关答题记录） |
| `/api/admin/import-problems` | POST | 批量导入题目（JSON 格式） |
| `/api/admin/knowledge-points` | GET | 获取所有知识点 |
| `/api/admin/stats` | GET | 统计面板（总数、难度分布、知识点分布、最近添加） |

### 5.8 其他路由

| 路由 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 重定向到登录页面 |
| `/<path>` | ANY | 兜底路由：OPTIONS 返回 204（CORS 预检），其他返回 404 |

---

## 六、核心业务逻辑详解

### 6.1 个性化选题策略（`AI_operate/exam.py`）

#### 选题优先级排序（SQL 层 `_fetch_ranked_candidates`）

```
优先级 1（最高）：从未做过（last_time IS NULL）
优先级 2（次高）：久未做（last_time < NOW() - INTERVAL stale_days DAY）
优先级 3（最低）：最近做过

同级内按 last_time 升序 + RAND() 增加多样性
```

#### 个性化推荐分配（`get_personalized_problems`）

```
70% 弱点题目：熟练度 < 0.6 的知识点
30% 强点题目：熟练度 ≥ 0.6 的知识点
  ├── 整体实力 ≥ 0.6 → 偏好「中等 / 困难」
  └── 整体实力 < 0.6 → 偏好「简单 / 中等」

候选不足时，从全量题库补齐
```

#### 筛选选题（`get_filtered_problems`）

在个性化基础上，额外支持按知识点集合 + 难度集合过滤，同样遵循"未做/久未做优先"排序。

### 6.2 能力画像系统（`AI_operate/Ability_Profile.py`）

每次提交答案后执行 4 步流水线：

```
答题记录入库（核心步骤，立即 commit）
    ↓ 失败不阻塞
更新能力画像（答对 +0.1，答错 -0.05，UPSERT）
    ↓ 失败不阻塞
聚合学习会话（24h 内连续答题归入同一会话，否则新建）
    ↓ 失败不阻塞
更新用户整体实力（所有知识点熟练度的算术平均值）
```

#### 熟练度更新公式

- 答对：`new_level = min(1.0, current_level + 0.1)`
- 答错：`new_level = max(0.0, current_level - 0.05)`

答对的增益（+0.1）是答错惩罚（-0.05）的两倍，鼓励正向学习。

#### AI 分析报告

调用 DeepSeek 大模型，输入用户能力画像 + 最近 10 条答题记录，生成包含以下内容的分析：

1. 强项和弱项知识点
2. 学习进展评估
3. 相关学习资源推荐（平台名称、资源标题、创作者）
4. 加强学习建议

### 6.3 学习会话聚合策略

```
查找用户最近一次会话：
  ├── 存在 且 end_time 在 24 小时内 → 追加到该会话（end_time 更新为 NOW()，计数器 +1）
  └── 不存在 或 已超 24 小时 → 创建新会话
```

### 6.4 用户整体实力计算

```
user_strength = Σ(各知识点熟练度) / 知识点数量

实力等级映射：
  0.00 ~ 0.39 → 初级（红色）
  0.40 ~ 0.69 → 中级（橙色）
  0.70 ~ 0.89 → 高级（绿色）
  0.90 ~ 1.00 → 专家（深绿色）
```

### 6.5 后台数据清理

- 独立守护线程，模块导入时自动启动
- 清理间隔：每小时（可配置 `CLEANUP_INTERVAL_SECONDS`）
- `user_answers` 保留 60 天（可配置 `RETAIN_DAYS_ANSWERS`）
- `learning_sessions` 保留 180 天（可配置 `RETAIN_DAYS_SESSIONS`）

---

## 七、前端架构详解

### 7.1 页面关系图

```
                    ┌─────────────┐
                    │  login.html  │  ← 入口：登录/注册
                    └──────┬──────┘
                           │ 登录成功
                           ▼
              ┌────────────────────────────┐
              │     dashboard.html          │  ← ★ 主容器
              │  ┌──────────────────────┐   │
              │  │ 导航卡片（4个）       │   │
              │  │ 做题 │画像│错题│聊天  │   │
              │  └──────────────────────┘   │
              │  ┌──────────────────────┐   │
              │  │ 做题区（examFrame）   │   │  ← exam.html (iframe)
              │  │ 画像区（内联渲染）    │   │  ← 雷达图/条形图/环形图
              │  │ 错题区（内联渲染）    │   │  ← 错题列表 + AI 讲解
              │  │ 聊天区（chatFrame）   │   │  ← chat.html (iframe)
              │  └──────────────────────┘   │
              └────────────────────────────┘

  ┌──────────────┐     ┌──────────────┐
  │  exam.html   │     │  chat.html   │  ← 可独立打开的全功能页面
  │  （独立做题） │     │  （独立聊天） │
  └──────────────┘     └──────────────┘

  ┌──────────────┐
  │  admin.html  │  ← 管理后台（独立页面）
  └──────────────┘
```

### 7.2 JS 模块依赖关系

```
utils.js  ← 通用工具（防抖、格式化、Toast 提示）
    │
storage.js ← localStorage 封装（用户状态、聊天记录、缓存）
    │
api.js  ← HTTP 请求客户端（所有后端接口封装）
    │
    ├── auth.js      ← 登录/注册逻辑
    ├── exam.js      ← 做题页逻辑（选题、答题、AI 讲解流式渲染）
    ├── dashboard.js ← 仪表盘主控（模块切换、画像渲染、错题管理、跨 iframe 通信）
    ├── chat.js      ← 聊天页逻辑（多轮对话、流式读取、历史管理）
    └── admin.js     ← 管理后台（题目 CRUD、批量导入、统计面板）

renderer.js ← 独立模块：Markdown 解析 + 代码语法高亮（marked.js + Prism.js）
```

### 7.3 仪表盘模块切换机制

```
用户点击导航卡片 → switchSection(targetId)
    │
    ├── 隐藏所有模块（添加 .hidden 类）
    ├── 显示目标模块
    ├── iframe 懒加载：首次切换到做题/聊天模块时才设置 src 属性
    └── 自动触发数据加载：
        ├── profileSection → loadProfile()（含 16min 内存缓存）
        └── wrongSection   → loadWrongAnswers()
```

### 7.4 跨模块通信（Dashboard ↔ exam iframe）

Dashboard 通过 `postMessage` 向 iframe 内的 exam.html 发送指令：

```javascript
// 按筛选条件加载题目（从能力画像/错题本触发）
examFrame.contentWindow.postMessage({
  type: 'loadProblemsByFilter',
  payload: {
    count: 10,
    knowledgePoints: ['TCP 协议'],
    difficulties: ['中等', '困难']
  }
}, '*')

// 直接设置指定题目列表（错题重做）
examFrame.contentWindow.postMessage({
  type: 'setProblems',
  payload: { problems: [...] }
}, '*')
```

### 7.5 缓存策略

| 数据 | 缓存位置 | TTL | 说明 |
|------|----------|-----|------|
| 能力画像 | 内存 + localStorage | 16 分钟 | 登录时后台预加载 |
| 知识点列表 | 内存 | 30 分钟 | 仪表盘和做题页共用 |
| 用户信息 | localStorage | 永久 | currentUser, currentUserId, currentName |
| 聊天记录 | localStorage | 永久 | 按用户 + 对话 ID 分组 |
| 用户头像 | localStorage | 永久 | Base64 编码的图片数据 |

---

## 八、数据流全景

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户操作流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ① 登录                                                           │
│     login.html → POST /api/login                                  │
│     → localStorage 存储 currentUser / currentUserId / currentName │
│                                                                   │
│  ② 做题                                                           │
│     exam.html → GET /api/problems (带 user_id)                    │
│     → 后端执行个性化/随机选题策略                                   │
│     → 返回题目列表 → 前端渲染题目卡片                               │
│                                                                   │
│  ③ 提交答案                                                       │
│     POST /api/submit_answer                                       │
│     → 判题（对比正确答案）                                         │
│     → 更新能力画像（熟练度 ±0.1/-0.05）                            │
│     → 聚合学习会话（24h 窗口）                                     │
│     → 更新用户整体实力                                             │
│     → 返回判题结果 → 前端显示正误                                  │
│                                                                   │
│  ④ 查看画像                                                       │
│     GET /api/user_profile/<id>                                    │
│     → 获取基本信息 + 能力画像 + AI 分析                            │
│     → 前端渲染雷达图/条形图/环形图 + 实力进度条                    │
│                                                                   │
│  ⑤ 查看错题                                                       │
│     GET /api/wrong_answers/<id>                                   │
│     → 分页返回错题列表                                             │
│     → 支持按知识点/同难度重练 → postMessage 到 exam iframe         │
│     → 支持 AI 流式讲解 → POST /api/explain/stream                 │
│                                                                   │
│  ⑥ AI 对话                                                        │
│     POST /api/chat (SSE stream)                                   │
│     → DeepSeek 流式返回 → 前端打字机渲染 + Markdown + 代码高亮    │
│                                                                   │
│  ⑦ 管理后台                                                       │
│     admin.html → CRUD 题目 + 批量导入 JSON/CSV + 统计面板          │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 九、部署与启动

### 9.1 环境要求

- Python 3.11+
- MySQL 8.x（本地运行，默认端口 3306）
- Windows 操作系统（当前 `run_all.bat` 为 Windows 脚本）

### 9.2 数据库配置

编辑 `server/app.py` 中的 `db_config`：

```python
db_config = {
    'host': 'localhost',
    'user': 'root',
    'password': '你的数据库密码',
    'database': 'wzyProjectDb'
}
```

### 9.3 一键启动

```bash
run_all.bat
```

该脚本依次执行：
1. 激活 Python 虚拟环境
2. 安装 Python 依赖（`pip install -r requirements.txt`）
3. 初始化数据库（执行建表 + 插入测试数据）
4. 启动 Flask 后端（端口 3001）
5. 启动静态文件服务器（端口 8888）
6. 浏览器打开 `http://127.0.0.1:8888/login.html`

### 9.4 手动启动

```bash
# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python database/init_db.py

# 启动后端
python server/app.py

# 启动静态文件服务（另开终端）
python -m http.server 8888 --directory static
```

### 9.5 DeepSeek API 配置

编辑 `AI_operate/deepseek_chat.py`：

```python
API_KEY = "你的 DeepSeek API 密钥"
```

---

## 十、配置参数说明

以下配置位于 `server/app.py` 顶部：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `CLEANUP_ENABLED` | True | 是否启用后台数据清理 |
| `CLEANUP_INTERVAL_SECONDS` | 3600 | 清理间隔（秒） |
| `RETAIN_DAYS_ANSWERS` | 60 | user_answers 保留天数 |
| `RETAIN_DAYS_SESSIONS` | 180 | learning_sessions 保留天数 |

以下配置位于 `AI_operate/Ability_Profile.py`：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 答对增益 | +0.1 | 熟练度增加量 |
| 答错惩罚 | -0.05 | 熟练度减少量 |
| 初始熟练度 | 0.5 | 新知识点的默认值 |
| 会话窗口 | 24 小时 | 连续答题聚合为同一会话的时间阈值 |

---

## 十一、已知问题与注意事项

1. **双后端历史遗留**：`server/auth.js` 和 `server/index.js` 是早期 Express.js 版本，使用明文密码验证。当前 Flask 版本已完全取代它们，但文件仍保留在代码库中。

2. **API 密钥硬编码**：`AI_operate/deepseek_chat.py` 中直接硬编码了 DeepSeek API 密钥，生产环境应改为环境变量注入。

3. **数据库密码硬编码**：`server/app.py` 和 `database/db_connector.py` 中均硬编码了 MySQL 密码 `555a555`，生产环境应统一管理。

4. **前端无构建流程**：所有 JS 为原生 ES6 类，通过 `<script>` 标签加载，无模块打包、压缩、Tree-shaking。

5. **跨域配置宽松**：全局 CORS 设置为 `Access-Control-Allow-Origin: *`，生产环境应限制为具体域名。

6. **测试数据与主题不匹配**：`insert_test_data.sql` 中的测试题是 C 语言题目，但系统默认的 AI 人设和知识点列表面向计算机网络，属于数据未及时更新。

7. **会话窗口硬编码**：学习会话聚合窗口为 24 小时，硬编码在 `Ability_Profile.py` 中，代码注释写的是"2 分钟"但实际判断为 24 小时。

---

## 十二、扩展建议

1. **引入 JWT 鉴权**：当前认证仅返回 user_id，无 token 机制，建议引入 JWT 或 session 认证。
2. **前端工程化**：引入构建工具（Vite/Webpack），支持 TypeScript、模块化开发。
3. **API 网关**：将 Flask 直接对外改为 Nginx 反向代理，统一端口。
4. **Docker 部署**：编写 Dockerfile + docker-compose，一键部署 MySQL + Flask + 静态文件。
5. **移动端适配**：当前 UI 为桌面端设计，可增加响应式布局支持移动端。
6. **测试覆盖**：目前无任何自动化测试，建议补充单元测试 + 接口测试。
